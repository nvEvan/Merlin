/**
 * Used to share data amongst screens
 * @author Antony Lulciuc
 */
import { UserData } from "../app/models/composite/user-data.composite";

export var shared = {
    data: {
        userData: UserData
    }
}